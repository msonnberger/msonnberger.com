import React from 'react';
import styled from 'styled-components';
import { Spacer, SectionTitle } from '@components';

const About = () => <SectionTitle>About Me</SectionTitle>;

export default About;
